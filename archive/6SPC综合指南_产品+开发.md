# 6SPC 智能质量分析系统
## 产品需求与开发指南（综合版）

**文档版本**：v1.0
**创建日期**：2025-02-22
**产品阶段**：MVP
**目标受众**：产品经理、开发团队、质量工程师

---

# 第一部分：产品需求（PRD）

## 1. 文档信息

| 字段 | 内容 |
|-----|------|
| 产品名称 | 6SPC 智能质量分析系统 |
| 产品代号 | 6SPC-Pro-Max |
| 所属行业 | 医疗器械制造（塑料注射部件） |
| 合规标准 | ISO 13485:2016、FDA 21 CFR 820、EMA MDR/IVDR |
| 核心价值 | 将手写 QC 检验记录自动数字化为合规的统计过程控制报告 |

---

## 2. 产品背景

### 2.1 行业痛点

**当前流程的问题**：
- **效率低下**：质量工程师每天花费 2-3 小时手动录入检验数据
- **易出错**：手动键盘输入错误率高达 3-5%，影响产品质量判断
- **合规风险**：纸质记录难以追溯，不符合 ISO 13485 电子记录要求
- **分析延迟**：数据无法实时统计，质量问题发现滞后

**目标用户场景**：
```
质量工程师收到 50 份原材料进货检验记录（手写扫描件）
→ 需要在 2 小时内完成数据录入和 6SPC 分析
→ 生成合规的 IQC 统计报告（包含 Cp/Cpk/Pp/Ppk）
→ 报告需经主管审核后存档（10 年保留期）
```

### 2.2 市场机会

- **市场规模**：中国医疗器械制造商 20,000+ 家，普遍存在类似痛点
- **数字化转型**：90% 的企业仍在使用纸质/Excel 方式管理质量数据
- **监管压力**：NMPA（中国药监局）加强电子记录合规性审查

---

## 3. 产品目标

### 3.1 核心目标（MVP）

**目标 1：效率提升**
- 将 50 份检验记录的处理时间从 **2 小时** 缩短至 **15 分钟**（**8x 效率提升**）

**目标 2：错误率降低**
- 数据录入错误率从 **3-5%** 降低至 **<0.5%**（通过 AI 识别 + 人工验证）

**目标 3：合规性**
- 生成的报告符合 **ISO 13485** 电子记录要求
- 支持 **FDA 21 CFR Part 11** 电子签名标准（V2 功能）

### 3.2 成功指标

| 指标 | 基线 | 目标 | 测量方式 |
|-----|------|------|---------|
| 单批次处理时间 | 120 分钟 | 15 分钟 | 系统计时 |
| 数据错误率 | 3-5% | <0.5% | 人工抽检 10% |
| 用户满意度 | N/A | ≥4.0/5.0 | NPS 问卷调查 |
| 日处理批次量 | 10 批次 | 50+ 批次 | 系统日志 |

---

## 4. 用户画像

### 4.1 主要用户：质量工程师（质量部 QC）

**基本信息**：
- 年龄：25-40 岁
- 学历：大专/本科（机械、材料、质量管理专业）
- 工作经验：2-10 年
- 工具使用：Excel、Word、ERP 系统

**核心需求**：
- 快速完成日常数据录入和统计计算
- 确保统计计算的正确性（Cp/Cpk 不能算错）
- 生成美观、合规的报告供主管审核

**痛点**：
- "每天重复录入数据太枯燥，容易出错"
- "Excel 公式经常被误删，不知道结果对不对"
- "主管说报告格式不统一，返工很烦"

**技术能力**：中等（会使用办公软件，但不是程序员）

### 4.2 次要用户：质量主管

**基本信息**：
- 年龄：30-50 岁
- 职责：审核质量报告、决策放行/拒收

**核心需求**：
- 快速判断批次是否合格（Cpk ≥ 1.33）
- 识别异常趋势（工具磨损、供应商质量波动）
- 合规性审查（数据完整性、可追溯性）

---

## 5. 用户故事

### Epic 1：智能 OCR 识别

**US-1.1 上传扫描件**
- **作为**质量工程师
- **我想要**拖拽上传 PDF/JPG 检验记录
- **以便**启动自动化数据处理流程

**验收标准**：
- 支持拖拽上传
- 支持文件格式：PDF、JPG、PNG
- 显示上传进度和文件预览
- 文件大小限制：<10MB

---

**US-1.2 AI 自动提取数据**
- **作为**质量工程师
- **我想要**系统自动识别表格中的测量数据
- **以便**减少手动录入工作量

**验收标准**：
- 自动识别批次号、零件名称、规格限（USL/LSL）
- 自动提取所有测量数值（目标识别率 >90%）
- 显示识别进度（"AI 正在处理..."）
- 识别完成时长 <30 秒（50 个数据点）

---

**US-1.3 智能修正 OCR 错误**
- **作为**质量工程师
- **我想要**系统自动修正常见识别错误
- **以便**减少我的校对工作

**验收标准**：
- 自动修正缺失小数点：`102` → `10.2`（根据 USL 推断）
- 自动剥离单位：`10.1mm` → `10.1`
- 自动替形似字符：`O` → `0`，`l` → `1`
- 高亮显示可疑值（超出 ±3σ）

---

### Epic 2：交互式验证

**US-2.1 数据编辑器**
- **作为**质量工程师
- **我想要**在表格中直接编辑数据
- **以便**修正 OCR 错误或补充漏测数据

**验收标准**：
- 类似 Excel 的可编辑表格
- 支持添加/删除行
- 编辑后自动重新计算统计量
- 支持撤销/重做

---

**US-2.2 实时统计预览**
- **作为**质量工程师
- **我想要**编辑数据时实时看到 Cpk 变化
- **以便**快速判断数据是否合理

**验收标准**：
- 修改数据后 <1 秒更新 Cpk/Pp/Cp/Ppk
- 高亮显示不合格指标（红色）
- 显示均值、标准差、最大值、最小值

---

**US-2.3 完整性检查**
- **作为**质量工程师
- **我想要**系统提醒我数据是否完整
- **以便**避免遗漏测量点

**验收标准**：
- 显示预期数据点数（如 n=50）
- 实际点数 <80% 时显示 ⚠️ 警告
- 高亮显示空行或无效值

---

### Epic 3：6 SPC 可视化分析

**US-3.1 三图基础分析（MVP）**
- **作为**质量工程师
- **我想要**查看单值图、X-bar 图、R-图
- **以便**快速判断过程稳定性

**验收标准**：
- **单值图**：显示所有数据点、USL/LSL 线
- **X-bar 图**：显示子组均值（n=5）、控制限（UCL/LCL）
- **R-图**：显示子组极差
- 图表交互：鼠标悬停显示数值、可缩放

---

**US-3.2 完整 6 SPC 分析（V1.5）**
- **作为**质量工程师
- **我想要**查看直方图、正态概率图、能力图
- **以便**进行完整的统计分析

**验收标准**：
- **直方图**：频数分布 + 正态拟合曲线
- **正态概率图**：Q-Q 图检验正态性
- **能力图**：正态分布曲线 + 规格限 + Cp/Cpk 显示
- 支持"快速查看"（3 图）vs"完整分析"（6 图）切换

---

**US-3.3 图表导出**
- **作为**质量工程师
- **我想要**导出图表为图片
- **以便**在 Word/PPT 中使用

**验收标准**：
- 支持导出格式：PNG、SVG
- 支持单图表导出或全部导出
- 导出分辨率 ≥300 DPI（用于打印）

---

### Epic 4：报告生成

**US-4.1 HTML 报告预览（MVP）**
- **作为**质量工程师
- **我想要**预览最终报告
- **以便**确认内容无误后生成正式报告

**验收标准**：
- 报告包含：批次信息、数据表格、统计摘要、6 个图表
- 响应式布局，支持打印预览（Ctrl+P）
- 显示报告生成时间和唯一编号

---

**US-4.2 PDF 报告导出（V2）**
- **作为**质量工程师
- **我想要**导出 PDF 格式的正式报告
- **以便**存档和发送邮件

**验收标准**：
- PDF 格式符合 FDA 21 CFR Part 11 要求
- 包含电子签名区域
- PDF 文件大小 <2MB
- 文件命名规则：`批次号_6SPC报告_日期.pdf`

---

**US-4.3 报告模板管理**
- **作为**质量主管
- **我想要**自定义报告模板
- **以便**符合公司品牌规范

**验收标准**：
- 支持上传公司 Logo
- 自定义报告标题和页脚
- 保存多个模板（如"IQC 模板"、"IPQC 模板"）

---

### Epic 5：数据管理

**US-5.1 历史记录查询**
- **作为**质量工程师
- **我想要**按批次号或日期查询历史报告
- **以便**快速参考之前的分析结果

**验收标准**：
- 支持按批次号、日期范围、零件名称搜索
- 显示报告列表（批次号、日期、Cpk 状态）
- 点击可查看历史报告详情

---

**US-5.2 数据导出**
- **作为**质量工程师
- **我想要**导出原始数据为 Excel
- **以便**进一步分析或存档

**验收标准**：
- 导出格式：.xlsx（带格式）
- 包含所有原始测量数据
- 包含计算得到的统计量

---

## 6. 功能结构与优先级

### 6.1 功能优先级（MoSCoW）

| 优先级 | 功能模块 | 说明 |
|-------|---------|------|
| **Must Have** | OCR 数据提取 | 核心功能，必须实现 |
| **Must Have** | 交互式数据编辑器 | 人工验证必需 |
| **Must Have** | Cp/Cpk/Pp/Ppk 计算 | 统计核心 |
| **Must Have** | 3 图基础分析 | 单值图 + X-bar + R-图 |
| **Must Have** | HTML 报告预览 | MVP 阶段不生成 PDF |
| **Should Have** | 异常值检测（3σ） | 提升用户体验 |
| **Should Have** | OCR 智能修正 | 减少人工编辑 |
| **Should Have** | 实时正态性检验 | 统计正确性保障 |
| **Could Have** | 完整 6 SPC 图表 | 高级分析需求 |
| **Could Have** | 历史记录查询 | 便捷性功能 |
| **Won't Have (MVP)** | PDF 导出 | V2 功能 |
| **Won't Have (MVP)** | 电子签名 | V2 功能（需认证集成） |
| **Won't Have (MVP)** | 多用户权限管理 | MVP 单机使用 |

### 6.2 信息架构

```
6SPC 智能质量分析系统
├── 首页（上传界面）
│   ├── 快速开始指南
│   └── 拖拽上传区域
│
├── 处理页面
│   ├── 左侧：原始扫描件预览
│   ├── 中间：数据编辑器
│   └── 右侧：实时统计摘要
│
├── 分析页面
│   ├── 三图基础分析（MVP）
│   │   ├── 单值读数图
│   │   ├── X-bar 控制图
│   │   └── R-控制图
│   └── 完整 6 SPC 分析（V1.5）
│       ├── 直方图
│       ├── 正态概率图
│       └── 能力图
│
└── 报告页面
    ├── HTML 预览（MVP）
    ├── 批准操作
    └── PDF 导出（V2）
```

---

## 7. 用户流程描述

### 7.1 核心流程：从扫描件到报告

```
┌─────────────────────────────────────────────────────────────┐
│                     主流程（Happy Path）                      │
└─────────────────────────────────────────────────────────────┘

1. [质量工程师] 登录系统
   ↓
2. [质量工程师] 拖拽上传检验记录扫描件（PDF）
   ↓
3. [系统] 显示"AI 正在处理..."进度条
   ↓
4. [系统] 完成识别，显示提取结果
   ↓
5. [质量工程师] 检查数据表格，修正 OCR 错误
   ↓
6. [系统] 实时更新 Cpk/Pp 等统计量
   ↓
7. [质量工程师] 切换到"分析"标签页，查看 3 个图表
   ↓
8. [质量工程师] 确认数据无误，点击"预览报告"
   ↓
9. [系统] 生成 HTML 报告预览
   ↓
10. [质量工程师] 点击"批准并归档"
    ↓
11. [系统] 保存报告记录，生成唯一编号
    ↓
12. [质量工程师] 下载 HTML 报告（或打印存档）
```

### 7.2 异常流程

**异常 A：OCR 识别率低**
- 系统提示："识别完整度 60%，建议手动补全数据"
- 用户手动补充缺失数据点

**异常 B：Cpk 不合格（<1.33）**
- 系统高亮显示红色 "FAIL"
- 提示："Cpk 低于 1.33，请联系质量主管评估"
- 用户仍可批准报告（由质量工程师判断）

**异常 C：数据非正态分布**
- 系统显示警告："数据可能不符合正态分布（Shapiro-Wilk p < 0.05）"
- 提示："Cpk/Cp 指数可能不准确，建议使用非参数方法"

---

## 8. 非功能需求

### 8.1 性能要求（MVP 阶段）

| 场景 | 响应时间目标 | 备注 |
|-----|-------------|------|
| 单批次处理（50 数据点） | < 60 秒（含 OCR） | OCR 时间占大头（~30s） |
| 数据编辑后统计刷新 | < 1 秒 | 用户无感知延迟 |
| 图表渲染 | < 2 秒 | 使用 Plotly 缓存 |
| HTML 报告生成 | < 3 秒 | Jinja2 模板渲染 |

**V2 优化目标**（非 MVP 范围）：
- 支持并发处理多个批次
- 缓存常用数据（如规格限）
- 数据库索引优化历史查询

### 8.2 可用性要求

- **易学性**：新用户在 10 分钟内完成首次报告（无需培训）
- **容错性**：支持撤销操作（Undo），避免误操作无法挽回
- **可访问性**：支持键盘快捷键（Ctrl+S 保存、Ctrl+Z 撤销）

### 8.3 兼容性要求

- **浏览器**：Chrome 90+、Edge 90+、Safari 14+
- **操作系统**：Windows 10/11、macOS 11+
- **屏幕分辨率**：最低 1366x768，推荐 1920x1080

### 8.4 安全性要求（MVP 基础）

- **API 密钥管理**：MinerU Token 存储在 `.env` 文件，不提交到 Git
- **数据隐私**：不上传用户数据到第三方云（除 OCR API）
- **输入验证**：防止 SQL 注入、XSS 攻击

### 8.5 可维护性要求

- **代码注释**：核心统计函数必须有中文注释说明统计原理
- **错误日志**：记录所有异常（OCR 失败、计算错误）
- **版本管理**：使用 Git，遵循语义化版本（Semantic Versioning）

---

## 9. 风险与依赖

### 9.1 技术风险

| 风险 | 影响 | 概率 | 缓解措施 |
|-----|------|------|---------|
| MinerU API 不稳定 | 高 | 中 | 1. 实现重试机制 2. 优雅降级到模拟数据 3. 备选 OCR 方案（Tesseract） |
| 统计计算错误 | 高 | 低 | 1. 单元测试覆盖 2. 与 Minitab 交叉验证 3. 代码 Review |
| 性能瓶颈 | 中 | 低 | 1. 异步处理 OCR 2. 前端缓存 3. V2 优化 |

### 9.2 业务风险

| 风险 | 影响 | 概率 | 缓解措施 |
|-----|------|------|---------|
| 用户不接受 AI 识别结果 | 高 | 高 | 1. 强调"人机协作"而非全自动 2. 提供便捷编辑器 3. 逐步提升识别率 |
| 合规性争议 | 高 | 低 | 1. 咨询法规专家 2. 保留人工审核环节 3. 审计日志完善 |
| 竞品跟进 | 中 | 中 | 1. 快速迭代 2. 强化用户体验 3. 考虑开源策略 |

### 9.3 外部依赖

| 依赖 | 类型 | 风险等级 | 备选方案 |
|-----|------|---------|---------|
| MinerU API | OCR 服务 | 高 | Tesseract、Azure OCR、Google Vision API |
| Streamlit | UI 框架 | 低 | Flask + Vue.js（如需深度定制） |
| SciPy/NumPy | 统计计算 | 低 | 自实现（不推荐） |

---

## 10. 版本规划

### 10.1 MVP 版本（v1.0）- 当前开发

**时间线**：4 周

**核心功能**：
- ✅ OCR 数据提取（MinerU 集成）
- ✅ 交互式数据编辑器
- ✅ Cp/Cpk/Pp/Ppk 计算
- ✅ 3 图基础分析（单值图、X-bar、R-图）
- ✅ HTML 报告预览
- ✅ 异常值检测（3σ）
- ✅ OCR 智能修正（缺失小数点、单位剥离）
- ✅ 实时正态性检验

**不做**：
- ❌ PDF 导出
- ❌ 电子签名
- ❌ 历史记录查询
- ❌ 用户权限管理

---

### 10.2 V1.5 版本 - 增强版

**时间线**：MVP 后 2-3 个月

**新增功能**：
- 📊 完整 6 SPC 图表（直方图、正态概率图、能力图）
- 📁 历史记录查询
- 📤 Excel 数据导出
- 🎨 多报告模板管理
- 🔔 邮件通知（报告生成后发送给主管）

---

### 10.3 V2.0 版本 - 专业版

**时间线**：V1.5 后 3-6 个月

**新增功能**：
- 📄 PDF 导出（符合 FDA 21 CFR Part 11）
- ✍️ 电子签名集成
- 👥 多用户权限管理（管理员、工程师、只读）
- 🔗 ERP 系统集成（自动获取批次信息）
- 📈 高级统计（非参数方法、Box-Cox 变换）
- 🌐 多语言支持（英文、日文）

---

## 11. 数据指标

### 11.1 业务指标（Business Metrics）

| 指标 | 计算方式 | 目标值 |
|-----|---------|-------|
| 日均处理批次数 | 每日生成的报告数 | ≥50 批次 |
| 用户活跃度 | DAU / MAU | ≥60% |
| 报告批准率 | 批准报告数 / 生成报告数 | ≥95% |
| OCR 编辑率 | 需要人工编辑的报告占比 | ≤30% |

### 11.2 产品指标（Product Metrics）

| 指标 | 计算方式 | 目标值 |
|-----|---------|-------|
| 识别准确率 | 正确识别数据点 / 总数据点 | ≥90% |
| 计算准确率 | 抽验统计量正确性 | 100% |
| 页面加载时间 | 首页完全加载时间 | <3 秒 |
| 崩溃率 | 系统异常退出次数 / 使用次数 | <0.1% |

### 11.3 统计学指标（Statistical Metrics）

| 指标 | 说明 | 验证方法 |
|-----|------|---------|
| Cp/Cpk 计算准确性 | 符合 ASTM E2281 标准 | 与 Minitab 对比验证 |
| 正态性检验准确性 | Shapiro-Wilk p 值计算 | 与 scipy 结果对比 |
| 控制限计算准确性 | X-bar/R 图 UCL/LCL | 手工计算验证 |

---

# 第二部分：开发技术指南

## 12. 技术架构概览

### 12.1 系统架构图

```
┌──────────────────────────────────────────────────────────────┐
│                        用户界面层                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │  Streamlit   │  │   HTML 报告   │  │  CLI 工具     │        │
│  │  Dashboard   │  │   预览模板    │  │  (main.py)   │        │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
└──────────────────────────────────────────────────────────────┘
                            ↓
┌──────────────────────────────────────────────────────────────┐
│                        业务逻辑层                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │  SPCEngine   │  │ OCRService   │  │ DataValidator│        │
│  │ (统计计算)    │  │ (OCR 集成)    │  │ (数据验证)    │        │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
└──────────────────────────────────────────────────────────────┘
                            ↓
┌──────────────────────────────────────────────────────────────┐
│                        外部服务层                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │  MinerU API  │  │  文件系统     │  │  (可选) 数据库│        │
│  │  v4          │  │  (本地存储)   │  │  (SQLite)    │        │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
└──────────────────────────────────────────────────────────────┘
```

### 12.2 核心数据流程

```
扫描上传 → OCR 提取 → 数据清洗 → SPC 计算 → 交互式验证 → HTML 报告
   ↓          ↓          ↓          ↓           ↓            ↓
 PDF文件   MinerU   智能修正   Cp/Cpk/    实时编辑     Jinja2
           API      3σ检测    Pp/Ppk      预览        模板
```

---

## 13. 核心组件详解

### 13.1 OCRService（`src/ocr_service.py`）

**职责**：MinerU API v4 客户端封装，文档数字化

**关键类和方法**：

```python
class MinerUClient:
    """MinerU API v4 客户端"""
    BASE_URL = "https://mineru.net/api/v4"

    def process_file(self, file_path):
        """
        完整处理流程：
        1. 请求上传 URL
        2. 上传文件（PUT）
        3. 创建提取任务
        4. 轮询结果
        返回：Markdown 格式的表格内容
        """
```

**OCR 逻辑保护**：
```python
def _parse_markdown_to_json(self, md):
    """
    Markdown 表格 → 结构化数据

    保护的错误类型：
    1. 缺失小数点：102 → 10.2（根据 USL 推断）
    2. 单位噪声：10.1mm → 10.1
    3. 形似字符：O → 0, l → 1
    4. 不完整提取：显示警告（实际点数 <80%）
    """
```

**优雅降级策略**：
```python
def extract_table_data(self, file_path):
    """
    优先使用 MinerU API
    失败时自动降级到模拟数据（_get_mock_data_multi）
    确保系统始终可用
    """
```

**开发注意事项**：
- API 密钥存储在 `.env` 文件（`OCR_API_KEY`）
- OCR 识别率目标：≥90%（印刷表格），70-90%（手写数据）
- 轮询间隔：5 秒，超时时间：5 分钟

---

### 13.2 SPCEngine（`src/spc_engine.py`）

**职责**：统计过程控制计算引擎

**关键类和方法**：

```python
class SPCEngine:
    def __init__(self, usl=None, lsl=None, target=None):
        self.usl = usl  # 上规格限
        self.lsl = lsl  # 下规格限
        self.target = target  # 目标值（可选）

    def calculate_stats(self, data, subgroup_size=5):
        """
        计算 SPC 统计量

        参数：
            data: 测量值列表
            subgroup_size: 子组大小（默认 5）

        返回：
            {
                "mean": 均值,
                "std_overall": 整体标准差,
                "std_within": 子组内标准差,
                "cp", "cpk", "pp", "ppk": 能力指数,
                "cpk_status": "PASS" | "FAIL",
                "subgroups": {"x_bar": [...], "r": [...], "size": 5}
            }
        """
```

**统计学原理**：

#### 1. 标准差计算（两种方法）

**整体标准差（std_overall）**：
```python
std_overall = np.std(arr, ddof=1)  # 样本标准差（n-1）
```
- 用途：Pp/Ppk 计算（长期性能）
- 反映所有变异来源（过程变异 + 批间变异）

**子组内标准差（std_within）**：
```python
r_bar = np.mean(r_data)  # 平均极差
d2 = 2.326  # n=5 时的常数
std_within = r_bar / d2
```
- 用途：Cp/Cpk 计算（潜在能力）
- 仅反映子组内变异（短期变异）

**d2 常数表**：
| 子组大小 n | d2 | A2 | D3 | D4 |
|-----------|----|----|----|---|
| 2 | 1.128 | 1.880 | 0 | 3.267 |
| 3 | 1.693 | 1.023 | 0 | 2.574 |
| 4 | 2.059 | 0.729 | 0 | 2.282 |
| 5 | 2.326 | 0.577 | 0 | 2.114 |
| 6 | 2.534 | 0.483 | 0 | 2.004 |

#### 2. 过程能力指数

**Cp（潜在能力）**：
```
Cp = (USL - LSL) / (6 × σ_within)
```
- 含义：规格宽度与 6 倍过程标准差的比值
- 假设：过程中心与规格中心重合

**Cpk（最小潜在能力）**：
```
Cpk = min[(USL - μ) / (3 × σ_within), (μ - LSL) / (3 × σ_within)]
```
- 含义：考虑过程中心偏移后的能力
- 实际使用：Cpk ≥ 1.33 = PASS

**Pp/Ppk**：公式同上，但使用 `σ_overall`

#### 3. 控制限计算（X-bar/R 图）

**X-bar 图控制限**：
```
UCL = X_double_bar + A2 × R_bar
LCL = X_double_bar - A2 × R_bar
```
- `X_double_bar`：子组均值的平均值
- `A2`：常数（n=5 时为 0.577）

**R 图控制限**：
```
UCL = D4 × R_bar
LCL = D3 × R_bar
```
- `D3`、`D4`：常数（n=5 时 D3=0, D4=2.114）

**开发注意事项**：
- 所有统计计算必须与 Minitab 结果验证（误差 <0.01）
- 添加单元测试覆盖边界条件（空列表、单值、常数数据）
- 正态性检验使用 `scipy.stats.shapiro`

---

### 13.3 Streamlit UI（`src/verify_ui.py`）

**职责**：人机交互验证仪表板

**核心功能模块**：

#### 1. 设计系统
```python
st.markdown("""
    <style>
    .stApp { background-color: #F0FDFA; }  /* 青色背景 */
    .stMetric {  /* 指标卡片样式 */
        background: rgba(255, 255, 255, 0.7);
        border-radius: 15px;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    }
    .premium-card {  /* 高端卡片样式 */
        background: white;
        padding: 30px;
        border-radius: 20px;
        border-left: 5px solid #0891B2;
    }
    </style>
""", unsafe_allow_html=True)
```

**配色方案**：
- 主色：`#0891B2`（青色）
- 辅色：`#134E4A`（深青色）
- 强调色：`#22C55E`（绿色，用于通过）
- 警告色：`#EF4444`（红色，用于失败）

#### 2. 数据编辑器
```python
df = pd.DataFrame({"Value": data["measurements"]})
edited_df = st.data_editor(
    df,
    num_rows="dynamic",  # 允许添加/删除行
    key=f"editor_{i}",
    use_container_width=True
)

# 实时重新计算
measurements = edited_df["Value"].tolist()
stats = engine.calculate_stats(measurements)
```

#### 3. 图表渲染（MVP：3 个图）

**单值读数图**：
```python
fig_ind = px.line(y=measurements, title=f"All {len(measurements)} Data Points")
fig_ind.update_traces(
    line_color="#22D3EE",
    line_width=2,
    mode='lines+markers'
)
fig_ind.add_hline(y=usl, line_dash="dash", line_color="#EF4444")
fig_ind.add_hline(y=lsl, line_dash="dash", line_color="#EF4444")
```

**X-bar 图**：
```python
fig_x = px.line(
    y=stats["subgroups"]["x_bar"],
    title=f"Subgroup Mean (n={stats['subgroups']['size']})"
)
fig_x.add_hline(y=usl, line_dash="dash", line_color="#EF4444")
fig_x.add_hline(y=stats["mean"], line_dash="solid", line_color="#22C55E")
```

**R-图**：
```python
fig_r = px.line(y=stats["subgroups"]["r"], title="Subgroup Range")
fig_r.update_traces(line_color="#22D3EE", line_width=3)
```

#### 4. 实时统计摘要
```python
m1, m2, m3, m4 = st.columns(4)
m1.metric("Cpk", f"{stats['cpk']:.3f}")
m2.metric("Ppk", f"{stats['ppk']:.3f}")
m3.metric("Status", stats["cpk_status"])
m4.metric("Mean", f"{stats['mean']:.4f}")
```

**开发注意事项**：
- 使用 `st.session_state` 缓存 OCR 结果（避免重复调用 API）
- 图表使用 Plotly（交互式，支持缩放）
- 大数据量时考虑分页显示（每页 50 行）

---

## 14. 数据结构规范

### 14.1 维度数据集（Dimension Set）

**用途**：OCR 返回 → SPC 消费

```python
{
    "header": {
        "batch_id": str,           # 批次标识符
        "dimension_name": str,     # 被测参数名称（如 "长度 mm"）
        "usl": float,              # 上规格限
        "lsl": float               # 下规格限
    },
    "measurements": List[float]    # 原始测量值
}
```

**示例**：
```python
{
    "header": {
        "batch_id": "D1-Length",
        "dimension_name": "长度 (mm)",
        "usl": 10.5,
        "lsl": 9.5
    },
    "measurements": [10.1, 10.2, 9.9, 10.0, 10.3, ...]
}
```

### 14.2 SPC 计算结果（SPC Results）

**用途**：SPCEngine 返回 → UI 显示

```python
{
    "mean": float,                 # 均值
    "std_overall": float,          # 整体标准差
    "std_within": float,           # 子组内标准差
    "min": float,                  # 最小值
    "max": float,                  # 最大值
    "count": int,                  # 样本量

    # 能力指数
    "cp": float,                   # 潜在能力指数
    "cpk": float,                  # 最小潜在能力
    "pp": float,                   # 整体性能指数
    "ppk": float,                  # 最小整体性能
    "cpk_status": "PASS" | "FAIL", # 合规状态（Cpk ≥ 1.33）

    # 子组数据（用于 X-bar/R 图）
    "subgroups": {
        "x_bar": List[float],      # 子组均值
        "r": List[float],          # 子组极差
        "size": int                # 子组大小（默认 5）
    }
}
```

---

## 15. 6 SPC 图表完整实现指南

### 15.1 MVP 阶段（3 个图表，已实现）

#### 图表 1：单值读数图（Individual Readings Plot）

**用途**：展示所有原始测量数据点，直观反映数据分布和趋势

**实现代码**：
```python
import plotly.express as px

fig_ind = px.line(
    y=measurements,
    title=f"All {len(measurements)} Data Points",
    labels={"y": "Measurement", "x": "Sample Number"}
)

# 样式配置
fig_ind.update_traces(
    line_color="#22D3EE",
    line_width=2,
    mode='lines+markers',
    marker=dict(size=4, color="#134E4A")
)

# 添加规格限
fig_ind.add_hline(
    y=usl,
    line_dash="dash",
    line_color="#EF4444",
    annotation_text="USL"
)
fig_ind.add_hline(
    y=lsl,
    line_dash="dash",
    line_color="#EF4444",
    annotation_text="LSL"
)

# 布局配置
fig_ind.update_layout(
    plot_bgcolor='white',
    paper_bgcolor='rgba(0,0,0,0)',
    height=300,
    margin=dict(l=10, r=10, t=30, b=10),
    hovermode='x unified'
)

st.plotly_chart(fig_ind, use_container_width=True)
```

**关键元素**：
- ✅ 折线图显示所有数据点
- ✅ USL/LSL 规格限虚线（红色）
- ✅ 数据点标记显示所有测量值
- ✅ 鼠标悬停显示具体数值

**统计意义**：
- 快速识别异常值（超出规格限）
- 检测数据漂移（系统性偏移）
- 发现测量系统问题（如读数波动异常）

---

#### 图表 2：X-bar 图（均值控制图）

**用途**：监控子组均值的变化，检测过程中心的偏移

**实现代码**：
```python
# 获取子组均值数据
x_bar_data = stats["subgroups"]["x_bar"]
subgroup_size = stats["subgroups"]["size"]

fig_x = px.line(
    y=x_bar_data,
    title=f"Subgroup Mean (n={subgroup_size})",
    labels={"y": "Subgroup Mean", "x": "Subgroup Number"}
)

# 样式配置
fig_x.update_traces(
    line_color="#0891B2",
    line_width=3,
    mode='lines+markers',
    marker=dict(color="#134E4A", size=8)
)

# 添加规格限
fig_x.add_hline(y=usl, line_dash="dash", line_color="#EF4444", annotation_text="USL")
fig_x.add_hline(y=lsl, line_dash="dash", line_color="#EF4444", annotation_text="LSL")

# 添加均值线
fig_x.add_hline(
    y=stats["mean"],
    line_dash="solid",
    line_color="#22C55E",
    annotation_text="MEAN"
)

# 计算并添加控制限（UCL/LCL）
# UCL = X_double_bar + A2 * R_bar
# LCL = X_double_bar - A2 * R_bar
# （A2 常数：n=5 时为 0.577）
x_double_bar = np.mean(x_bar_data)
r_bar = np.mean(stats["subgroups"]["r"])
A2 = 0.577  # n=5

ucl = x_double_bar + A2 * r_bar
lcl = x_double_bar - A2 * r_bar

fig_x.add_hline(y=ucl, line_dash="dot", line_color="#F59E0B", annotation_text="UCL")
fig_x.add_hline(y=lcl, line_dash="dot", line_color="#F59E0B", annotation_text="LCL")

# 布局配置
fig_x.update_layout(
    plot_bgcolor='white',
    paper_bgcolor='rgba(0,0,0,0)',
    height=300,
    margin=dict(l=10, r=10, t=30, b=10)
)

st.plotly_chart(fig_x, use_container_width=True)
```

**关键元素**：
- ✅ 子组均值折线（n=5，可配置）
- ✅ USL/LSL 规格限（红色虚线）
- ✅ 总均值线（绿色实线）
- ✅ 控制限（UCL/LCL，橙色点线）

**统计意义**：
- **中心线**（X-double-bar）：表示过程平均性能
- **点超出控制限**：表明过程失控（需调查原因）
- **连续点在同一侧**：提示系统性偏移（如工具磨损）
- **控制限 vs 规格限**：
  - 控制限：反映过程固有变异（Voice of the Process）
  - 规格限：反映客户要求（Voice of the Customer）

**失控规则（Western Electric Rules）**：
1. 1 个点超出 3σ
2. 连续 3 个点中有 2 个超出 2σ
3. 连续 5 个点中有 4 个超出 1σ
4. 连续 8 个点在中心线同一侧

---

#### 图表 3：R-图（极差控制图）

**用途**：监控子组内变异，检测过程一致性的变化

**实现代码**：
```python
r_data = stats["subgroups"]["r"]

fig_r = px.line(
    y=r_data,
    title="Subgroup Range",
    labels={"y": "Range", "x": "Subgroup Number"}
)

# 样式配置
fig_r.update_traces(
    line_color="#22D3EE",
    line_width=3,
    mode='lines+markers',
    marker=dict(color="#134E4A", size=8)
)

# 计算并添加控制限
# UCL = D4 * R_bar
# LCL = D3 * R_bar
# （D3、D4 常数：n=5 时 D3=0, D4=2.114）
r_bar = np.mean(r_data)
D3, D4 = 0, 2.114  # n=5

ucl_r = D4 * r_bar
lcl_r = D3 * r_bar

fig_r.add_hline(y=r_bar, line_dash="solid", line_color="#22C55E", annotation_text="R-bar")
fig_r.add_hline(y=ucl_r, line_dash="dot", line_color="#F59E0B", annotation_text="UCL")
if lcl_r > 0:
    fig_r.add_hline(y=lcl_r, line_dash="dot", line_color="#F59E0B", annotation_text="LCL")

# 布局配置
fig_r.update_layout(
    plot_bgcolor='white',
    paper_bgcolor='rgba(0,0,0,0)',
    height=300,
    margin=dict(l=10, r=10, t=30, b=10)
)

st.plotly_chart(fig_r, use_container_width=True)
```

**关键元素**：
- ✅ 子组极差折线
- ✅ R-bar 中心线（绿色实线）
- ✅ 控制限（UCL/LCL，橙色点线）

**统计意义**：
- **R 图失控**：表明过程稳定性恶化（如原材料变异、设备问题）
- **R 图受控 + X-bar 图失控**：过程中心偏移（易调整）
- **R 图失控 + X-bar 图失控**：过程整体不稳定（需大修）
- **必须联合分析**：R 图是 X-bar 图的前提（先看 R 图，再看 X-bar 图）

---

### 15.2 V1.5 阶段（新增 3 个图表）

#### 图表 4：直方图（Histogram）

**用途**：展示数据分布形状，验证正态分布假设

**实现代码**：
```python
import plotly.graph_objects as go
from scipy.stats import norm

# 计算频数分布
hist, bin_edges = np.histogram(measurements, bins=20)
bin_centers = (bin_edges[:-1] + bin_edges[1:]) / 2

# 创建直方图
fig_hist = go.Figure()

# 添加频数柱状图
fig_hist.add_trace(go.Bar(
    x=bin_centers,
    y=hist,
    name='Frequency',
    marker_color='#0891B2',
    opacity=0.7
))

# 添加正态拟合曲线
x_fit = np.linspace(min(measurements), max(measurements), 100)
y_fit = norm.pdf(x_fit, stats["mean"], stats["std_overall"])
# 缩放到直方图高度
scale_factor = len(measurements) * (bin_edges[1] - bin_edges[0])
y_fit_scaled = y_fit * scale_factor

fig_hist.add_trace(go.Scatter(
    x=x_fit,
    y=y_fit_scaled,
    mode='lines',
    name='Normal Fit',
    line=dict(color='#EF4444', width=2)
))

# 添加规格限和均值线
fig_hist.add_vline(x=usl, line_dash="dash", line_color="#EF4444", annotation_text="USL")
fig_hist.add_vline(x=lsl, line_dash="dash", line_color="#EF4444", annotation_text="LSL")
fig_hist.add_vline(x=stats["mean"], line_dash="solid", line_color="#22C55E", annotation_text="MEAN")

# 布局配置
fig_hist.update_layout(
    title="Histogram with Normal Distribution Fit",
    xaxis_title="Measurement",
    yaxis_title="Frequency",
    plot_bgcolor='white',
    paper_bgcolor='rgba(0,0,0,0)',
    height=400,
    bargap=0.1
)

st.plotly_chart(fig_hist, use_container_width=True)
```

**关键元素**：
- ✅ 频数分布柱状图（20 个分箱）
- ✅ 正态分布拟合曲线（红色）
- ✅ USL/LSL 规格限标记
- ✅ 均值线

**统计意义**：
- **对称分布**：数据围绕均值对称分布 → 正态分布假设成立
- **偏态分布**：左偏或右偏 → 数据不满足正态假设，Cpk 指数可能不准确
- **双峰分布**：两个峰值 → 可能存在混合过程（如两台设备、两个供应商）
- **评估过程中心与规格中心的偏移**：均值越接近规格中心越好

**正态性检验**：
```python
from scipy.stats import shapiro

statistic, p_value = shapiro(measurements)

if p_value < 0.05:
    st.warning(f"⚠️ 数据可能不符合正态分布 (Shapiro-Wilk p={p_value:.4f})")
    st.info("建议：考虑数据变换（Box-Cox）或使用非参数方法")
else:
    st.success(f"✅ 数据符合正态分布假设 (Shapiro-Wilk p={p_value:.4f})")
```

---

#### 图表 5：正态概率图（Normal Probability Plot / Q-Q Plot）

**用途**：检验数据是否符合正态分布（分位数-分位数图）

**实现代码**：
```python
from scipy import stats
import matplotlib.pyplot as plt

# 创建 Q-Q 图
fig, ax = plt.subplots(figsize=(8, 6))

stats.probplot(measurements, dist="norm", plot=ax)

# 自定义样式
ax.get_lines()[0].set_color('#0891B2')  # 数据点
ax.get_lines()[0].set_marker('o')
ax.get_lines()[0].set_markersize(4)
ax.get_lines()[1].set_color('#EF4444')  # 参考线
ax.get_lines()[1].set_linewidth(2)

ax.set_title('Normal Probability Plot (Q-Q Plot)', fontsize=14, color='#134E4A')
ax.set_xlabel('Theoretical Quantiles', fontsize=12)
ax.set_ylabel('Ordered Values', fontsize=12)
ax.grid(True, alpha=0.3)

# 转换为 Streamlit 可显示格式
st.pyplot(fig)

# 添加统计检验
statistic, p_value = stats.shapiro(measurements)
st.metric("Shapiro-Wilk p-value", f"{p_value:.4f}")
```

**关键元素**：
- ✅ 观测值分位数 vs. 理论正态分位数散点图
- ✅ 参考直线（理论正态分布）
- ✅ Shapiro-Wilk 检验 p 值

**统计意义**：
- **数据点沿直线分布** → 正态分布假设成立
- **系统性偏离直线** → 非正态分布，需考虑数据变换
- **尾部偏离** → 存在异常值或混合分布
- **S 形曲线** → 数据分布比正态更陡峭（峰度 > 3）
- **倒 S 形曲线** → 数据分布比正态更平坦（峰度 < 3）

**解读规则**：
| 模式 | 含义 | 处理建议 |
|------|------|---------|
| 沿直线分布 | 正态分布 | 继续使用 Cp/Cpk |
| 左端向下弯曲 | 左偏分布 | 考虑对数变换 |
| 右端向上弯曲 | 右偏分布 | 考虑平方根变换 |
| 两端向上弯曲 | 轻尾分布 | 使用非参数方法 |
| 两端向下弯曲 | 重尾分布 | 检查异常值 |

---

#### 图表 6：过程能力图（Capability Plot / Six-Pack）

**用途**：综合展示过程能力指数和规格限关系

**实现代码**：
```python
import plotly.graph_objects as go
from scipy.stats import norm

# 创建 X 轴（测量值范围）
x = np.linspace(
    stats["mean"] - 4 * stats["std_overall"],
    stats["mean"] + 4 * stats["std_overall"],
    100
)

# 计算正态分布概率密度
y = norm.pdf(x, stats["mean"], stats["std_overall"])

fig_cap = go.Figure()

# 添加正态分布曲线
fig_cap.add_trace(go.Scatter(
    x=x,
    y=y,
    mode='lines',
    name='Process Distribution',
    line=dict(color='#0891B2', width=3),
    fill='tozeroy',  # 填充区域
    fillcolor='rgba(8, 145, 178, 0.2)'
))

# 添加规格限垂直线
fig_cap.add_vline(
    x=usl,
    line_dash="dash",
    line_color="#EF4444",
    line_width=2,
    annotation_text=f"USL={usl}",
    annotation_position="top left"
)

fig_cap.add_vline(
    x=lsl,
    line_dash="dash",
    line_color="#EF4444",
    line_width=2,
    annotation_text=f"LSL={lsl}",
    annotation_position="top left"
)

# 添加均值线
fig_cap.add_vline(
    x=stats["mean"],
    line_dash="solid",
    line_color="#22C55E",
    line_width=2,
    annotation_text=f"Mean={stats['mean']:.3f}"
)

# 计算超出规格的概率（PPM - Parts Per Million）
ppm_usl = (1 - norm.cdf(usl, stats["mean"], stats["std_overall"])) * 1e6
ppm_lsl = norm.cdf(lsl, stats["mean"], stats["std_overall"]) * 1e6
total_ppm = ppm_usl + ppm_lsl

# 添加能力指数文本
annotation_text = f"""
<b>Capability Indices:</b>
Cp = {stats['cp']:.3f}
Cpk = {stats['cpk']:.3f}
Pp = {stats['pp']:.3f}
Ppk = {stats['ppk']:.3f}

<b>Out of Spec:</b>
Total PPM = {total_ppm:.0f}
Above USL = {ppm_usl:.0f} PPM
Below LSL = {ppm_lsl:.0f} PPM
"""

fig_cap.add_annotation(
    text=annotation_text,
    xref="paper",
    yref="paper",
    x=0.98,
    y=0.98,
    showarrow=False,
    bgcolor="rgba(255, 255, 255, 0.9)",
    bordercolor="#0891B2",
    borderwidth=2,
    borderpad=5,
    font=dict(size=11)
)

# 布局配置
fig_cap.update_layout(
    title="Process Capability Analysis",
    xaxis_title="Measurement",
    yaxis_title="Probability Density",
    plot_bgcolor='white',
    paper_bgcolor='rgba(0,0,0,0)',
    height=500,
    showlegend=True
)

st.plotly_chart(fig_cap, use_container_width=True)
```

**关键元素**：
- ✅ 正态分布曲线（基于均值和标准差）
- ✅ USL/LSL 垂直标记线
- ✅ 目标值线（Target，如有）
- ✅ Cp、Cpk、Pp、Ppk 指数显示
- ✅ 超出规格的概率百分比（PPM）

**统计意义**：
- **可视化过程分布与规格的关系**：
  - 曲线在规格限内 → 过程能力充足
  - 曲线超出规格限 → 存在不良品
- **直观展示改进空间**：
  - 如果曲线偏移 → 调整过程中心（降低均值偏移）
  - 如果曲线过宽 → 减少过程变异（改进标准差）
- **哑铃图对比**（Before/After）：
  - 改进前：宽曲线 + 偏移
  - 改进后：窄曲线 + 居中

**PPM（Parts Per Million）计算**：
```python
# 超出 USL 的概率
ppm_usl = (1 - norm.cdf(USL, mean, std)) * 1e6

# 低于 LSL 的概率
ppm_lsl = norm.cdf(LSL, mean, std) * 1e6

# 总不良率
total_ppm = ppm_usl + ppm_lsl
```

**能力等级判定**（行业通用标准）：
| Cpk 值 | 能力等级 | 评价 | 不良率（PPM） |
|--------|---------|------|--------------|
| < 1.0 | 不足 | 需立即改进 | > 2700 PPM |
| 1.0 - 1.33 | 勉强合格 | 需持续改进 | 2700 - 63 PPM |
| 1.33 - 1.67 | 合格 | 满足要求 | 63 - 0.57 PPM |
| 1.67 - 2.0 | 优秀 | 过程稳定 | 0.57 - 0.002 PPM |
| > 2.0 | 卓越 | 六西格玛水平 | < 0.002 PPM |

---

### 15.3 图表组合使用策略

#### MVP 阶段布局
```
┌─────────────────────────────────────────────────────┐
│              6SPC 智能质量分析系统                      │
├─────────────────────────────────────────────────────┤
│  上传区 → 数据编辑器 → 统计摘要                       │
├─────────────────────────────────────────────────────┤
│  📊 单值读数图  │  📈 X-bar 图  │  📉 R-图         │
└─────────────────────────────────────────────────────┘
```

#### V1.5 阶段布局（2x3 网格）
```
┌─────────────────────────────────────────────────────┐
│              6SPC 智能质量分析系统 (完整版)             │
├─────────────────────────────────────────────────────┤
│  📊 单值读数图  │  📈 X-bar 图  │  📉 R-图         │
├─────────────────────────────────────────────────────┤
│  📊 直方图      │  📈 正态概率图 │  📊 能力图       │
└─────────────────────────────────────────────────────┘
```

#### 显示模式切换
```python
view_mode = st.radio(
    "选择查看模式",
    ["快速查看（3 图）", "完整分析（6 图）"],
    horizontal=True
)

if view_mode == "快速查看（3 图）":
    # 显示单值图、X-bar 图、R-图
else:
    # 显示全部 6 个图表
```

---

## 16. MVP 数据验证功能实现

### 16.1 异常值检测（3σ 原则）

**用途**：自动识别超出 ±3σ 的可疑数据点

**实现代码**：
```python
def detect_outliers(data, threshold=3.0):
    """
    检测异常值（基于 3σ 原则）

    参数：
        data: 测量值列表
        threshold: 标准差倍数（默认 3）

    返回：
        outliers_idx: 异常值索引列表
        outliers_val: 异常值列表
    """
    arr = np.array(data)
    mean = np.mean(arr)
    std = np.std(arr, ddof=1)

    upper_limit = mean + threshold * std
    lower_limit = mean - threshold * std

    outliers_idx = np.where((arr > upper_limit) | (arr < lower_limit))[0]
    outliers_val = arr[outliers_idx]

    return outliers_idx.tolist(), outliers_val.tolist()

# 在 UI 中使用
outliers_idx, outliers_val = detect_outliers(measurements)

if outliers_val:
    st.warning(f"⚠️ 检测到 {len(outliers_val)} 个异常值（超出 ±3σ）")
    st.write("异常值索引：", outliers_idx)
    st.write("异常值：", outliers_val)
else:
    st.success("✅ 未检测到异常值")
```

**增强功能**：高亮显示异常值
```python
# 在数据编辑器中高亮异常值
df = pd.DataFrame({"Value": measurements, "Status": ["正常"] * len(measurements)})

for idx in outliers_idx:
    df.at[idx, "Status"] = "⚠️ 异常"

# 使用条件样式显示
def highlight_outliers(val):
    if "异常" in str(val):
        return 'background-color: #FFE4E6'
    return ''

st.dataframe(
    df.style.applymap(highlight_outliers, subset=['Status']),
    use_container_width=True
)
```

---

### 16.2 OCR 误读智能修正

**用途**：根据规格限自动推断缺失小数点

**实现代码**：
```python
def smart_correction(value, usl, lsl):
    """
    智能修正 OCR 误读

    规则：
    1. 如果值 > USL * 10，尝试除以 10（缺失小数点）
    2. 如果值包含非数字字符，剥离（单位噪声）
    3. 如果值包含形似字符，替换（O → 0, l → 1）

    参数：
        value: OCR 识别的原始值
        usl: 上规格限
        lsl: 下规格限

    返回：
        修正后的值
    """
    # 规则 1：缺失小数点
    if isinstance(value, (int, float)) and value > usl * 10:
        corrected = value / 10
        # 检查修正后是否在合理范围内
        if lsl <= corrected <= usl:
            return corrected

    # 规则 2：剥离单位（字符串转数值）
    if isinstance(value, str):
        import re
        # 提取数字部分
        numbers = re.findall(r"[-+]?\d*\.\d+|\d+", value)
        if numbers:
            return float(numbers[0])

    # 规则 3：形似字符替换
    if isinstance(value, str):
        value = value.replace('O', '0').replace('o', '0')
        value = value.replace('l', '1').replace('I', '1')
        try:
            return float(value)
        except ValueError:
            pass

    return value

# 批量修正数据
def correct_measurements(measurements, usl, lsl):
    corrected = []
    corrections = []  # 记录修正历史

    for i, val in enumerate(measurements):
        original = val
        corrected_val = smart_correction(val, usl, lsl)
        corrected.append(corrected_val)

        if corrected_val != original:
            corrections.append({
                "index": i,
                "original": original,
                "corrected": corrected_val,
                "rule": "缺失小数点推断"
            })

    return corrected, corrections

# 在 UI 中显示修正历史
if corrections:
    st.info(f"🔧 自动修正了 {len(corrections)} 处 OCR 误读")
    st.dataframe(pd.DataFrame(corrections))
```

---

### 16.3 实时正态性检验

**用途**：用户编辑数据时自动检验正态分布假设

**实现代码**：
```python
from scipy.stats import shapiro, anderson

def normality_test(data, alpha=0.05):
    """
    正态性检验（Shapiro-Wilk + Anderson-Darling）

    参数：
        data: 测量值列表
        alpha: 显著性水平（默认 0.05）

    返回：
        {
            "method": "Shapiro-Wilk" | "Anderson-Darling",
            "statistic": 统计量,
            "p_value": p 值,
            "is_normal": True | False,
            "message": 说明文字
        }
    """
    # Shapiro-Wilk 检验（样本量 < 5000）
    if len(data) < 5000:
        statistic, p_value = shapiro(data)
        is_normal = p_value > alpha

        message = (
            f"Shapiro-Wilk 检验："
            f"统计量={statistic:.4f}, "
            f"p={p_value:.4f}"
        )

        return {
            "method": "Shapiro-Wilk",
            "statistic": statistic,
            "p_value": p_value,
            "is_normal": is_normal,
            "message": message
        }

    # Anderson-Darling 检验（大样本）
    else:
        result = anderson(data)
        # 使用 5% 显著性水平的临界值
        critical_value = result.critical_values[2]  # 5%
        is_normal = result.statistic < critical_value

        message = (
            f"Anderson-Darling 检验："
            f"统计量={result.statistic:.4f}, "
            f"临界值(5%)={critical_value:.4f}"
        )

        return {
            "method": "Anderson-Darling",
            "statistic": result.statistic,
            "p_value": None,  # Anderson-Darling 不返回 p 值
            "is_normal": is_normal,
            "message": message
        }

# 在 UI 中实时显示
result = normality_test(measurements)

if result["is_normal"]:
    st.success(f"✅ 数据符合正态分布假设")
    st.caption(result["message"])
else:
    st.warning(f"⚠️ 数据可能不符合正态分布")
    st.caption(result["message"])
    st.info("建议：考虑数据变换（Box-Cox）或使用非参数方法")
```

**增强功能**：Box-Cox 变换建议
```python
from scipy.stats import boxcox

def suggest_boxcox(data):
    """
    Box-Cox 变换（将非正态数据转换为正态）

    返回：
        transformed_data: 变换后的数据
        lambda_value: 最优 λ 值
    """
    # Box-Cox 要求数据 > 0
    if min(data) <= 0:
        # 平移数据到正数
        data_shifted = [x + abs(min(data)) + 0.01 for x in data]
    else:
        data_shifted = data

    # 执行 Box-Cox 变换
    transformed, lambda_value = boxcox(data_shifted)

    return transformed, lambda_value

# 显示变换建议
if not result["is_normal"]:
    if st.button("查看 Box-Cox 变换建议"):
        transformed, lambda_val = suggest_boxcox(measurements)

        col1, col2 = st.columns(2)
        with col1:
            st.write("**原始数据**")
            st.pyplot(create_histogram(measurements))

        with col2:
            st.write(f"**变换后数据** (λ = {lambda_val:.2f})")
            st.pyplot(create_histogram(transformed))

        # 检验变换后数据
        result_transformed = normality_test(transformed)
        if result_transformed["is_normal"]:
            st.success("✅ 变换后数据符合正态分布")
        else:
            st.warning("⚠️ 变换后仍不符合正态分布，建议使用非参数方法")
```

---

## 17. 构建与部署

### 17.1 本地开发环境

**环境配置**：
```bash
# 1. 创建虚拟环境
python3 -m venv .venv
source .venv/bin/activate  # macOS/Linux
# 或
.venv\Scripts\activate  # Windows

# 2. 安装依赖
pip install -r requirements.txt

# 3. 配置环境变量
cp .env.example .env
# 编辑 .env 文件，添加 OCR_API_KEY

# 4. 运行 CLI 测试
python3 main.py

# 5. 启动 Streamlit Dashboard
python3 -m streamlit run src/verify_ui.py
```

**依赖清单**（requirements.txt）：
```
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
plotly>=5.15.0
streamlit>=1.25.0
python-dotenv>=1.0.0
requests>=2.31.0
Pillow>=10.0.0
jinja2>=3.1.2
```

---

### 17.2 生产部署（Streamlit Cloud）

**部署步骤**：
```bash
# 1. 代码推送到 GitHub
git init
git add .
git commit -m "Initial commit: 6SPC MVP"
git push origin main

# 2. 在 Streamlit Cloud 部署
# 访问 https://share.streamlit.io/
# 连接 GitHub 仓库
# 设置环境变量：OCR_API_KEY

# 3. 访问应用
# https://your-app-name.streamlit.app/
```

**性能优化建议**：
```python
# src/verify_ui.py

# 启用缓存（加速重复计算）
@st.cache_data(ttl=3600)  # 缓存 1 小时
def load_historical_data():
    """加载历史数据"""
    pass

# 异步处理（避免阻塞 UI）
import asyncio

async def process_ocr_async(file_path):
    """异步 OCR 处理"""
    loop = asyncio.get_event_loop()
    result = await loop.run_in_executor(None, ocr.extract_table_data, file_path)
    return result
```

---

## 18. 测试策略

### 18.1 单元测试

**测试框架**：`pytest`

**示例测试**：
```python
# tests/test_spc_engine.py

import pytest
import numpy as np
from src.spc_engine import SPCEngine

def test_cpk_calculation():
    """测试 Cpk 计算准确性"""
    # 已知数据集
    data = [10.1, 10.2, 9.9, 10.0, 10.3] * 10  # n=50
    engine = SPCEngine(usl=10.5, lsl=9.5)

    stats = engine.calculate_stats(data)

    # 验证 Cpk 值（与 Minitab 对比）
    assert abs(stats['cpk'] - 1.67) < 0.01

def test_outlier_detection():
    """测试异常值检测"""
    from src.utils import detect_outliers

    # 包含异常值的数据
    data = [10.0, 10.1, 10.2, 15.0, 10.0]  # 15.0 是异常值
    outliers_idx, outliers_val = detect_outliers(data)

    assert 15.0 in outliers_val
    assert 3 in outliers_idx

def test_normality_test():
    """测试正态性检验"""
    from src.utils import normality_test

    # 正态分布数据
    np.random.seed(42)
    normal_data = np.random.normal(10, 0.5, 50).tolist()

    result = normality_test(normal_data)
    assert result["is_normal"] == True
```

**运行测试**：
```bash
pytest tests/ -v --cov=src --cov-report=html
```

---

### 18.2 集成测试

**测试场景**：
```python
# tests/test_integration.py

def test_end_to_end_workflow():
    """测试完整工作流程"""
    from src.ocr_service import OCRService
    from src.spc_engine import SPCEngine

    # 1. OCR 提取
    ocr = OCRService()
    data = ocr.extract_table_data("sample_scan.pdf")

    assert len(data) > 0
    assert "measurements" in data[0]

    # 2. SPC 计算
    measurements = data[0]["measurements"]
    engine = SPCEngine(
        usl=data[0]["header"]["usl"],
        lsl=data[0]["header"]["lsl"]
    )
    stats = engine.calculate_stats(measurements)

    assert stats["cpk"] >= 0
    assert stats["cpk_status"] in ["PASS", "FAIL"]
```

---

### 18.3 性能测试

**测试工具**：`timeit`

```python
# tests/test_performance.py

import timeit

def test_ocr_performance():
    """测试 OCR 处理时间"""
    from src.ocr_service import OCRService

    ocr = OCRService()

    def process():
        return ocr.extract_table_data("sample_scan.pdf")

    time = timeit.timeit(process, number=1)

    assert time < 60  # 应在 60 秒内完成

def test_spc_calculation_performance():
    """测试统计计算时间"""
    from src.spc_engine import SPCEngine
    import numpy as np

    data = np.random.normal(10, 0.5, 1000).tolist()
    engine = SPCEngine(usl=10.5, lsl=9.5)

    def calculate():
        return engine.calculate_stats(data)

    time = timeit.timeit(calculate, number=100)

    # 100 次计算应在 1 秒内完成
    assert time < 1.0
```

---

## 19. 附录

### 19.1 术语表

| 术语 | 英文 | 解释 |
|-----|------|------|
| 6SPC | Six Sigma Statistical Process Control | 六西格玛统计过程控制 |
| Cp/Cpk | Process Capability Index | 过程能力指数（潜在能力） |
| Pp/Ppk | Process Performance Index | 过程性能指数（整体性能） |
| USL/LSL | Upper/Lower Specification Limit | 上/下规格限 |
| X-bar/R Chart | Mean and Range Control Chart | 均值-极差控制图 |
| OCR | Optical Character Recognition | 光学字符识别 |
| IQC | Incoming Quality Control | 进料检验 |
| ISO 13485 | International Organization for Standardization 13485 | 医疗器械质量管理体系标准 |
| Shapiro-Wilk | Shapiro-Wilk Test | 正态性检验方法 |
| PPM | Parts Per Million | 百万分之一（不良率单位） |

---

### 19.2 统计学公式参考

**Cp（潜在能力）**：
```
Cp = (USL - LSL) / (6 × σ_within)
```

**Cpk（最小潜在能力）**：
```
Cpk = min[(USL - μ) / (3 × σ_within), (μ - LSL) / (3 × σ_within)]
```

**Pp（整体性能）**：
```
Pp = (USL - LSL) / (6 × σ_overall)
```

**Ppk（最小整体性能）**：
```
Ppk = min[(USL - μ) / (3 × σ_overall), (μ - LSL) / (3 × σ_overall)]
```

其中：
- `USL/LSL`：上/下规格限
- `μ`：过程均值
- `σ_within`：子组内标准差（使用 R-bar/d2 估计）
- `σ_overall`：整体标准差（使用所有数据计算）

---

### 19.3 参考资料

- **ISO 13485:2016**：Medical devices - Quality management systems
- **FDA 21 CFR Part 11**：Electronic Records; Electronic Signatures
- **ASTM E2281**：Standard Practice for Process Capability and Performance Measurement
- **AIAG Statistical Process Control (SPC) Reference Manual**
- **Western Electric Rules**：Statistical Quality Control Handbook (1956)
- **Montgomery, D.C.**：Introduction to Statistical Quality Control (7th Edition)

---

### 19.4 开发工作流

**新增统计指标**：
1. 在 `src/spc_engine.py` 中的 `SPCEngine` 类添加计算方法
2. 更新 `calculate_stats()` 返回字典
3. 在 `src/verify_ui.py` 中添加 UI 显示
4. 添加单元测试验证计算准确性

**新增图表**：
1. 在 `src/verify_ui.py` 中添加 Plotly 图表代码
2. 使用 `st.columns()` 或 `st.tabs()` 组织布局
3. 添加交互功能（缩放、悬停提示）
4. 测试图表在不同数据量下的渲染性能

**修改 OCR 逻辑**：
1. 在 `src/ocr_service.py` 的 `_parse_markdown_to_json()` 中添加解析规则
2. 更新 `_get_mock_data_multi()` 提供模拟数据
3. 测试 OCR 识别率（目标 ≥90%）
4. 添加错误处理和重试机制

**UI 改进**：
1. 遵循现有的设计系统（青色主题 `#0891B2`、圆角卡片）
2. 使用 Streamlit 内置组件（`st.metric`、`st.data_editor`、`st.plotly_chart`）
3. 确保响应式布局（`use_container_width=True`）
4. 添加加载状态（`st.spinner()`、`st.progress()`）

---

### 19.5 常见问题（FAQ）

**Q: Cpk < 1.33 时系统如何处理？**
A: 系统标记为 "FAIL" 并高亮显示红色，但不阻止用户批准报告（由质量工程师判断）。

**Q: 如何处理非正态分布数据？**
A: MVP 阶段显示警告并建议 Box-Cox 变换。后续迭代支持非参数方法（如中位数能力指数）。

**Q: OCR 识别率如何？**
A: 取决于扫描质量。印刷表格识别率 >95%，手写数据约 70-90%，需要人工验证。

**Q: 系统是否支持多语言？**
A: MVP 仅中文。V2.0 计划支持英文、日文（通过 `st.i18n` 实现）。

**Q: 如何确保计算准确性？**
A: 所有统计公式与 Minitab 交叉验证，单元测试覆盖边界条件，误差 <0.01。

**Q: 数据隐私如何保护？**
A: 数据仅存储在本地，不上传到云端（除 MinerU OCR API）。后续版本支持本地 OCR（Tesseract）。

---

## 20. 版本历史

| 版本 | 日期 | 作者 | 变更说明 |
|-----|------|------|---------|
| v1.0 | 2025-02-22 | Claude Code | 初始版本，MVP 范围定义，包含 3 图基础分析 |

---

**文档结束**

© 2025 6SPC 智能质量分析系统 | 保留所有权利
